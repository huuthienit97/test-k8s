version: 1
layout: multi
services:
  - name: api
    path: backend
    ingress: /api
    health: /health
  - name: web
    path: frontend
    ingress: /
    health: /
  - name: worker
    path: worker
    expose: false
    health: /health
